import { Map, fromJS } from 'immutable';

export function getImmutableObject(obj) {
  return fromJS(obj);
}import { Map, fromJS } from 'immutable';

export function getImmutableObject(obj) {
  return fromJS(obj);
}
